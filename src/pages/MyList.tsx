import React from 'react';
import Wrapper from "../sections/Wrapper";

const MyList = () => {
    return (
        <div className="list">
            
        </div>
    );
};

export default Wrapper(MyList);